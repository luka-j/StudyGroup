<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rs.luka.android.studygroup" >

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.recyclers.RootActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.recyclers.GroupActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.RootActivity" />
        </activity>
        <activity
            android:name=".activities.recyclers.CourseActivity"
            android:label="@string/lessons_title"
            android:launchMode="singleTop">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.GroupActivity" />
        </activity>
        <activity
            android:name=".activities.recyclers.LessonActivity"
            android:label="@string/details_title"
            android:launchMode="singleTop">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.CourseActivity" />
        </activity>
        <activity
            android:name=".activities.singleitemactivities.NotePagerActivity"
            android:label="@string/notes">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.LessonActivity" />
        </activity>
        <activity
            android:name=".activities.singleitemactivities.QuestionPagerActivity"
            android:label="@string/questions">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.LessonActivity" />
        </activity>
        <activity
            android:name=".activities.singleitemactivities.AddGroupActivity"
            android:label="@string/add_group"
            android:windowSoftInputMode="stateVisible">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.RootActivity" />
        </activity>
        <activity
            android:name=".activities.singleitemactivities.AddCourseActivity"
            android:label="@string/add_course"
            android:windowSoftInputMode="stateVisible">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.GroupActivity" />
        </activity>
        <activity
            android:name=".activities.singleitemactivities.AddNoteActivity"
            android:label="@string/add_note"
            android:windowSoftInputMode="stateVisible">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.LessonActivity" />
        </activity>
        <activity
            android:name=".activities.singleitemactivities.AddQuestionActivity"
            android:label="@string/add_question"
            android:windowSoftInputMode="stateVisible">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.recyclers.LessonActivity" />
        </activity>
    </application>

</manifest>
